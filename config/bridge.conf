# Installed as /etc/sailfish-sensors/bridge.conf.  Values are consumed by the
# systemd tunnel and relay services; tokens and SSH keys live in separate files.
SFS_HOST=192.168.0.100
SFS_PHONE_USER=nemo
SFS_PHONE_PORT=8765
SFS_LOCAL_PORT=18765
SFS_PHONE_GPS_PORT=8766
SFS_LOCAL_GPS_PORT=18766
SFS_GPSD_FEED_PORT=2948
SFS_MOUNT_MATRIX=1,0,0,0,1,0,0,0,1
